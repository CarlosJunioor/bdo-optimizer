# Static CRT so the shipped exe runs on machines without the VC++
# redistributable: Windows does not install VCRUNTIME140.dll by default.
[target.x86_64-pc-windows-msvc]
rustflags = ["-C", "target-feature=+crt-static"]
